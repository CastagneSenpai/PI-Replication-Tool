<UserControl x:Class="Views.ConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Viewmodel="clr-namespace:ViewModels" d:DataContext="{d:DesignInstance Type=Viewmodel:ConnectionViewModel}"
             mc:Ignorable="d"
        d:DesignHeight="600" d:DesignWidth="1200"
        Height="Auto" Width="Auto"
        Background="#FFFFFF">
    <UserControl.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
    </UserControl.Resources>
    
    <UserControl.DataContext>
        <Viewmodel:ConnectionViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="68*" />
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="550"/>
            <ColumnDefinition Width="500"/>
        </Grid.ColumnDefinitions>

        <ListView Grid.Row="1" Grid.Column="0" 
                 x:Name="listSourceServer" 
                 ItemsSource="{Binding ListSourceServer}" 
                 SelectedItem="{Binding SelectedSourceServer}" 
                 HorizontalAlignment="Center" 
                 VerticalAlignment="Center" 
                 Width="245" 
                 RenderTransformOrigin="0.5,0.5"/>

        <ListView Grid.Row ="1" Grid.Column="1" 
                  x:Name="listTargetServer" 
                  ItemsSource="{Binding ListTargetServer}" 
                  SelectedItem="{Binding SelectedTargetServer}" 
                  HorizontalAlignment="Center" 
                  VerticalAlignment="Center" 
                  Width="245" 
                  RenderTransformOrigin="0.5,0.5"/>

        <Button Grid.Row="2" Grid.Column="1"
                Content="Connect"
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Width="100"
                Height="20"
                Command="{Binding ButtonConnectSourceServer}">
        </Button>

        <Image Grid.Row="2" Grid.Column="1" Source="/Assets/Cross.png" Margin="228,12,58,12" />
        <Image Grid.Row="2" Grid.Column="1" Source="/Assets/Check.png" Margin="228,12,58,12" />

        <Button Grid.Row="2" Grid.Column="2"
                Content="Connect"
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Width="100"
                Height="20"
                Command="{Binding ButtonConnectTargetServer}">
        </Button>

        <Image Grid.Row="2" Grid.Column="2" Source="/Assets/Cross.png" Margin="228,12,58,12" />
        <Image Grid.Row="2" Grid.Column="1" Source="/Assets/Check.png" Margin="228,12,58,12" />
    </Grid>
</UserControl>
