<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      throwExceptions="false"
      internalLogLevel="Off" internalLogFile="c:\temp\nlog-internal.log">
<!--xsi:schemaLocation="http://www.nlog-project.org/schemas/NLog.xsd NLog.xsd"-->
  
  <!-- optional, add some variables
  https://github.com/nlog/NLog/wiki/Configuration-file#variables
  -->
  <variable name="myvar" value="myvalue"/>

  <!--
  See https://github.com/nlog/nlog/wiki/Configuration-file
  for information on customizing logging rules and outputs.
   -->
  <targets>

    <!--
    add your targets here
    See https://github.com/nlog/NLog/wiki/Targets for possible targets.
    See https://github.com/nlog/NLog/wiki/Layout-Renderers for the possible layout renderers.
    -->

    <!--
    Write events to a file with the date in the filename.
    <target xsi:type="File" name="f" fileName="${basedir}/logs/${shortdate}.log"
            layout="${longdate} ${uppercase:${level}} ${message}" />
            
    -->
    <!--<target name="logfile" xsi:type="File" fileName="${basedir}/logs/${shortdate}_logfile.txt" layout="${longdate}|${level:uppercase=true}|${logger}|${threadid}|${message}|${exception:format=tostring}" />-->
    <target 
      xsi:type="File"
      name="logfile"  
      fileName="E:\PIReplicationTool\Logs\${shortdate}_logfile.txt" 
      layout="${longdate}|${level:uppercase=true}|${logger}|${threadid}|${message}|${exception:format=tostring}" />
    
    <target 
      xsi:type="Console" 
      name="logconsole" 
      layout="${level:uppercase=true}|${logger}|${message}"/>

      <!-- RichTextBox to display logs in WPF application --> 
      <target xsi:type="RichTextBox"
              name="logwindow"
              layout="${level:uppercase=true}|${logger}|${message}"
              height="12"
              autoScroll="false"
              maxLines="1"
              showMinimized="false"
              toolWindow="true"
              controlName="LogControl"
              formName="formName"
              width="12"
              useDefaultRowColoringRules="true"
              allowAccessoryFormCreation="true"
              messageRetention="None"
              supportLinks="false"
>

        <!-- repeated word-colorings -->
        <word-coloring backgroundColor=""
                       fontColor="black" 
                       ignoreCase="false"
                       text="String"
                       wholeWords="false"/>

        <!-- repeated row-colorings-->
        <row-coloring backgroundColor=""
                      fontColor="black"/>
      </target>
    
  </targets>

  <rules>
    <!-- add your logging rules here -->

    <!--
    Write all events with minimal level of Debug (So Debug, Info, Warn, Error and Fatal, but not Trace)  to "f"
    <logger name="*" minlevel="Debug" writeTo="f" />
    -->
    <logger name="PIReplicationToolLogger" minlevel="Debug" writeTo="logfile, logconsole" />
    <logger name="PIReplicationToolLogger" minlevel="Info" writeTo="logfile, logconsole" />
    <logger name="PIReplicationToolLogger" minlevel="Warning" writeTo="logfile, logconsole" />
    <logger name="PIReplicationToolLogger" minlevel="Error" writeTo="logfile, logconsole" />
    
  </rules>


  <rules>
    
  </rules>
</nlog>